redeyed_sentinel.settings:
  type: config_object
  label: 'Redeyed Sentinel CAPTCHA settings'
  mapping:
    site_key:
      type: string
      label: 'Site key (public)'
    api_key:
      type: string
      label: 'API key (secret)'
    base_url:
      type: string
      label: 'Base URL'
