redeyed_sentinel.settings:
  type: config_object
  label: 'Redeyed Sentinel CAPTCHA settings'
  mapping:
    site_key:
      type: string
      label: 'Site key (public)'
    secret_key:
      type: string
      label: 'Secret key'
    base_url:
      type: string
      label: 'Base URL'
